# AGENTS.md —— 2024 A 题"板凳龙" 自动加载入口

> 本文件由 opencode 在每次进入本目录时**自动读取**，是 AI 理解本项目、定位文献与代码的**唯一必需入口**。
> 其余文档均为按需加载（读入口 → 索引 → 深挖）。

## 这是什么项目

2024 年全国大学生数学建模竞赛 **A 题《"板凳龙"闹元宵》** 模拟赛目录。
223 节板凳首尾相连构成"板凳龙" = **广义 N 拖车（N-trailer）铰接链**，沿阿基米德螺线盘入盘出，龙头前把手恒速 1 m/s。
关键几何：板宽 30 cm、孔径 5.5 cm、孔距板端 27.5 cm；龙头板长 3.41 m（两孔中心距 2.86 m），龙身/尾板长 2.20 m（两孔中心距 1.65 m）；螺线 `r = b·θ`，`b = 0.55/(2π)`。

五问（Q1–Q5 **全部已解**）：
- **Q1** 0–300 s 每秒整条龙 224 把手位置/速度 → 已解，结果见 `附件/result1.xlsx`
- **Q2** 盘入终止时刻（板凳不碰撞）→ 已解，`t*=412.47 s`，首次碰撞龙头(1)↔第9节，结果见 `附件/result2.xlsx`
- **Q3** 最小螺距（盘入调头空间边界）→ 已解，`p*≈0.4503 m`，见 `model/problem3*.md`
- **Q4** S 形调头（p=1.7m，R1=2R2）+ 全龙 −100~100s 仿真 + 可否缩短 → 已解，`R1=3.0054m/R2=1.5027m/γ=173.12°/L0=13.6212m`；**两弧族内长度不变**（不变性定理），弧-线-弧结构可缩短至 `L*=10.0642m (−26.11%)`，见 `model/problem4*.md`、`附件/result4.xlsx`
- **Q5** 龙头最大速度（各把手≤2 m/s）→ 已解，`v*=1.246266 m/s`；三模型（A 采样缩放/B 事件分解/C 求根+认证）互证 `R_max=1.604793379`；峰 `u1*=14.4800m`，把手 **4–8 并列**（同弧恒速），机理=跨F缝 1.5542×跨T缝 1.0325；弧-线-弧权衡：R→D/2 临界退化 `ρ_alt≥22.7 → v*→0`，见 `model/problem5*.md`

## 自动加载顺序（每次进目录执行）

1. 读本文件（AGENTS.md）→ 恢复上下文；
2. 文献相关 → 先读 `docs/AI_INDEX.md`（定位）→ 细节再读 `docs/LITERATURE_REVIEW.md`；
3. 模型/求解相关 → 读 `model/problem1.md`、`model/problem1_solve.md`、`model/problem2.md`、`model/problem2_solve.md`、`model/problem3.md`、`model/problem3_solve.md`；
4. 代码/脚本 → 读 `src/` 下脚本（自包含，可直接运行）；
5. 脚本化读取文献元数据 → 解析 `docs/index.json`；
6. 涉及回忆/继续/迁移 → 按 ChatMem 流程（见 `C:\Users\极光\.config\opencode\AGENTS.md`）。

## 文献可用性（重要，避免误读/幻觉引用）

`docs/` 共 6 篇，**仅 4 篇可读**：

| 可用 | 不可用（损坏，待重下） |
|---|---|
| C3 Tanaka 三节拖车（38页，铰接链相对角递推 `θ̇ᵢ=(ν/L)sinθᵢ`）| C1 Michalek N-trailer（实为 ScienceDirect 错误页 HTML）|
| E1 Lekkas 费马螺线（16页，`r=k√θ`，曲率连续，Q4 调头设计）| E3 Reeds-Shepp（截断 PDF，0 页）|
| F1 Verscheure 凸规划（10页，SOCP 时间最优，Q5）||
| F2 Artuñedo jerk-limited（13页，`v_SLC=min{v_max,√(a_lat/κ)}`，Q5）||

**规则**：引用 C1/E3 时必须标注"原文待核实，基于文件名/领域知识"，不得当作已核实原文；不要浪费时间修复 E3（已试过 pypdf/pikepdf/pymupdf 均失败）。

## 目录结构速查

```
模拟/2024/
├── AGENTS.md              # 本文件（自动加载）
├── A题.pdf                # 赛题原文（3 页）
├── docs/                  # 参考文献库
│   ├── LITERATURE_REVIEW.md   # 三大主题综述 + 对比 + Q1–Q5 映射
│   ├── AI_INDEX.md            # 可读索引：逐篇页码定位、提取命令
│   ├── index.json             # 机器可读索引（公式/元数据/Q1–Q5 映射）
│   ├── AGENT_MEMORY.md        # 持久记忆：术语表、进展、导航协议
│   └── AGENTS.md
├── model/                 # 模型与求解文档
│   ├── problem1.md            # Q1 模型建立（螺线运动学 + 逆推法 + 速度）
│   ├── problem1_solve.md      # Q1 求解（细步长 + 实体碰撞检测 + 结果表）
│   ├── problem2.md            # Q2 碰撞检测完善（缺陷分析 + 修正 + CCD）
│   ├── problem2_solve.md      # Q2 求解（终止时刻 + 结果表 + 验证）
│   ├── problem3.md            # Q3 最小螺距优化模型（一维黑盒约束 + 二分）
│   ├── problem3_solve.md      # Q3 求解（p*≈0.4503 m + 结果表 + 验证）
│   ├── problem4.md            # Q4 S形调头模型（复合轨迹逆推法 + 不变性定理 + 弧-线-弧优化）
│   ├── problem4_solve.md      # Q4 求解（R1/R2/L0 + 5时刻表 + 缩短26.11% + 碰撞验证）
│   ├── problem5.md            # Q5 三级模型（齐次约化 + 弦切角传递 + 同弧恒速 + A/B/C 三模型）
│   └── problem5_solve.md      # Q5 求解（v*=1.246266 + 三模型互证 + 认证 + 弧-线-弧权衡）
├── src/                   # 自包含可运行脚本（python 直接运行）
│   ├── problem1_solve.py      # Q1：细步长求解 + SAT 碰撞检测（写 result1.xlsx）
│   ├── problem2_collision.py  # Q2：SAT/顶点包含/CCD + 二分（写 result2.xlsx）
│   ├── problem3_solve.py      # Q3：最小螺距二分搜索（SAT 判碰撞 + shapely 真实间距）
│   ├── problem4_solve.py      # Q4：S形几何 + -100~100s 仿真 + 不变性/弧-线-弧优化（写 result4.xlsx）
│   ├── problem4_figures.py    # Q4：4 张 Nature 风格图（示意/曲线族与优化/构型/速度）
│   ├── problem4_results.json  # Q4 全部关键数字（文档/绘图共用）
│   ├── problem5_solve.py      # Q5：三模型主流程（A 采样/B 事件/C 求根+Lipschitz 认证，~30min）
│   ├── problem5_certify.py    # Q5：峰窗事件子段穷举认证 + 弧-线-弧深弯区诊断（~4min）
│   ├── problem5_figures.py    # Q5：5 张 Nature 风格图（机理/比场/收敛/临界构型/权衡）
│   ├── problem5_results.json  # Q5 全部关键数字（solve+certify 共写，文档/绘图共用）
│   ├── explore_q5.py          # Q5：全域 0.25 粗扫（产出 _explore_q5.npz，图2b 数据源）
│   ├── _explore_q5.npz        # Q5 粗扫数组（grid/R/Iarg）
│   ├── flowcharts_bw.py       # 问题一/二/三算法流程图（黑白配色，像素级实测布局）
│   └── anim_dragon.py         # ≈盘入动态仿真 GIF
├── paper/figures/         # 全部示意图与动画
│   ├── anim_盘入仿真.gif         # 动态仿真（150帧）
│   ├── fig_算法流程图.png          # Q1 求解算法流程图
│   ├── fig_整龙盘入轨迹.png / fig_龙头* / fig_把手速度* / fig_龙尾*  # Q1 数据图
│   ├── fig_问题一碰撞间隙.png      # Q1 实体碰撞最小间隙 vs 时间
│   ├── fig_板凳几何尺寸示意.png / fig_盘入整体示意.png / fig_逆推法几何示意.png
│   ├── fig_矩形碰撞示意.png       # 三种碰撞情形（含缺陷1完全包含）
│   ├── fig_问题二算法流程.png      # 宽相+精相+二分
│   ├── fig_问题二数据结构.png      # 数据结构/函数组织
│   ├── fig_CCD连续碰撞示意.png    # CCD 隧道效应 + 黄金分割
│   ├── fig_碰撞临界时刻.png        # Q2 t*=412.47s 形态
│   ├── fig_问题三螺距搜索.png      # Q3 全程最小间距 vs 螺距（过零即 p*）
│   ├── fig_问题三临界形态.png      # Q3 p* 时龙头到达边界形态
│   ├── fig_问题三算法流程.png      # Q3 最小螺距二分搜索流程（黑白）
│   ├── fig_问题三首碰形态.png      # Q3 p<p* 时首碰形态（龙头↔第18节，交点×标注）
│   ├── fig_问题四S形调头示意.png   # Q4 主示意图（切点/圆心/半径/扫角）
│   ├── fig_问题四曲线族与优化.png  # Q4 两弧族不变性 + 弧-线-弧 L*(R1) 极小化
│   ├── fig_问题四全龙构型.png      # Q4 全景 + 5 关键时刻龙身构型快照（含最小间隙对）
│   ├── fig_问题四把手速度.png      # Q4 速度-时间 + 速度-把手编号（Nature 风格）
│   ├── fig_问题五速度传递机理.png  # Q5 跨缝放大弦切角几何 + 峰值构型弦切角分布
│   ├── fig_问题五速度比场.png      # Q5 比场热力图（事件线） + 全域包络与搜索域
│   ├── fig_问题五三模型收敛.png    # Q5 峰窗三模型对比 + 模型 A 网格收敛（假平台）
│   ├── fig_问题五临界构型.png      # Q5 峰值时刻板凳矩形 + 速度箭头（临界把手 4-8）
│   ├── fig_问题五结构权衡.png      # Q5 两曲线对比 + 长度-速度权衡（临界半径退化）
│   └── fig_算法流程图.png / fig_问题二算法流程.png  # Q1/Q2 流程（黑白重绘版）
└── 附件/                  # 结果文件（题目要求 6 位小数）
    ├── result1.xlsx            # Q1 每秒全龙位置/速度（位置+速度两 sheet）
    ├── result2.xlsx            # Q2 终止时刻 224 把手的 x/y/速度
    └── result4.xlsx            # Q4 -100~100s 每秒全龙 224 把手位置/速度（两 sheet）
```

## 当前完成度与关键结果

**已解决：**
- **Q1**：`b=0.55/(2π)`；弧长积分二分求龙头极角 + 逆推法（作圆取极角最近者）求 224 把手；细步长 dt=0.1s 中心差分求速（龙头 v≈0.999995≈1 m/s）；**实体碰撞检测**（板宽 0.30 m，SAT）0–300 s 最小间隙 1.95e-7 m，无碰撞；已写 `附件/result1.xlsx`。
- **Q2**：碰撞检测 = 宽相 AABB 预筛 + 精相（顶点包含 ∪ SAT）+ CCD（连续碰撞检测黄金分割）；二分求 `t*=412.47 s`，首次碰撞龙头(1)↔第9节；已写 `附件/result2.xlsx`。
- **Q4**：S 形调头曲线解析几何（切点 E/F、圆心 O1/O2、切点 T、扫角 γ、长度 L0 全闭式）→ 复合轨迹（螺线∪弧1∪弧2∪螺线）上逆推法递推 224 把手（-100~100s）；**两弧族内长度不变性定理**；**弧-线-弧**结构数值优化（网格+Nelder-Mead）缩短 26.11%；全程 SAT+shapely 碰撞验证无碰撞；已写 `附件/result4.xlsx`。
- **Q5**：**齐次性约化**（v_i = v·ρ_i(u1)，问题化为一元全局最大化 R_max = max ρ_i）→ **三模型**：A 网格采样缩放（暴露高估 +12.9% 与假平台）、B 接缝事件分解 + 光滑段 Brent 精确最大化、C 驻点求根 + Lipschitz 剪枝证书 + 事件子段穷举认证（宽 <1e-9）；`R_max=1.604793379`、**`v*=1.246266 m/s`**；峰 `u1*=14.479970m`、临界时刻 `t5*=11.618680s`、把手 4–8 并列（同弧恒速引理）；机理 = 跨F缝 1.554248 × 跨T缝 1.032521；验证：弦残差 1.3e-13、差分 9e-9、Q4 对照 0 偏差。

**关键数值（用于论文/校验）：**
- 龙头 t=0 位置：`(8.8, 0)`（第 16 圈）
- Q1 龙头速度：0.999995~0.999983 m/s（细步长）
- Q1 0–300 s 最小间隙：`1.95e-7 m`（无碰撞）
- Q2 终止时刻：`t* = 412.47 s`，精确值 `412.47009 s`（CCD）
- Q2 碰撞对：`(1, 9)`（龙头 ↔ 第 9 节）
- Q3 最小螺距：`p* ≈ 0.450348 m`（区间 0.450305–0.450391，精度 1e-4）
- Q3 临界碰撞对：龙头 ↔ 相邻圈第 18~20 节；首碰时龙头 r≈4.57~4.66 m
- Q3 入口：`θ_end = 62.7833 rad`（第 9.9923 圈），总弧长 `220.95 m`（≈221 s），入口曲率半径 `4.4994 m`
- Q4 基线（p=1.7m，R1=2R2）：`R1=3.005418 / R2=1.502709 m`，`γ=173.1184°`，`L0=13.621245 m`，入口/出口切点 `E=(-2.7119,-3.5911)`、`F=-E`，切点 `T=(0.90395,1.19703)`
- Q4 不变性定理：切点固定两弧族内 `R1+R2=4.508127 m`、`γ=3.021487 rad`、`L=13.621245 m` 与半径比例无关（6 种比例数值精确相等）
- Q4 弧-线-弧最优：`R1=0.8251 / R2=0.8978 m`，`α=99.43°`，`ℓ=7.0741 m`，`L*=10.0642 m`（**−26.11%**），`r_max=4.5`（不越界）
- Q4 碰撞验证：基线全程最小间隙 `0.4061 m`（板凳对 (1,17)，t≈29.95s）；优化曲线 `0.2734 m`（t≈13.75s），均无碰撞
- Q4 速度特征：整数秒采样最大 `1.4222 m/s @ t=14s`（第2把手）；连续峰值 `1.604793379 @ u1*=14.48m`（把手4–8并列）→ Q5 精确确认
- Q5 答案：`R_max=1.604793379`（峰窗子段穷举 + 剪枝证书裕度 0.32）、**`v* = 1.246266 m/s`**、`u1*=14.479970 m`、`t5*=11.618680 s`、临界把手 4–8 并列
- Q5 机理分解：跨F缝 `cos α1/cos β1 = 1.554248`（把手2陷弧2深弯 ~97°）× 跨T缝 `1.032521`；同弧内比值恒 1（偏差 0.0，浮点级）
- Q5 峰窗子段认证：6 子段段max = 1.0333 / 1.2240 / 1.5557 / **1.6048** / 1.4278 / 1.2235（支配裕度 ≥0.049）
- Q5 次级峰：`1.118373 @ u1=314.78`（链中段穿越 S 弯）；端部 ≤1.0038 单调回落
- Q5 弧-线-弧权衡：`R_alt 密扫 8.56 / 尖刺 22.75 @ u1≈12.03`；`v*_alt ≤ 0.088 m/s（−93%）`；根源 `R1→D/2=0.825` 临界半径雅可比退化（`|du|~(R−D/2)^{−1/2}→∞`）

**待办：**
- 重下 C1/E3 原文并更新综述/索引；
- 论文已定稿于 `paper/example.tex`（XeLaTeX 编译 41 页）：摘要恰 1 页；摘要后正文 30 页（含第十章灵敏度/鲁棒性、十一章评价精简版）；附录 A–F 含 Q1–Q5 核心代码。Q5 章为单模型口径（事件分解—驻点求根—双层认证）。灵敏度实验脚本 `src/problem5_sensitivity.py`（弦长±5%、k 扫描、xtol/h 稳定性、OU 波动蒙特卡洛 N=2000：满速 2% 波动超限概率 59%、0.95v*+2% 仅 0.9%）→ `problem5_sensitivity.json` + 图 `fig_问题五灵敏度鲁棒性.png`（`problem5_sens_fig.py`）。

## 环境与运行

- Windows / PowerShell；Python 3.12.3（conda）
- 依赖：`numpy`、`scipy`（brentq/minimize）、`openpyxl`（读写 xlsx）、`matplotlib`、`pillow`、`shapely`（真实间距交叉验证）
- 运行：`python src/problem1_solve.py`、`python src/problem2_collision.py`、`python src/problem3_solve.py`、`python src/problem4_solve.py`（+`problem4_figures.py`）、`python src/problem5_solve.py`（→`problem5_certify.py`→`problem5_figures.py`，需先跑 `explore_q5.py` 供图2b）、`python src/anim_dragon.py`
- 中文字体：matplotlib 需注册 `C:\Windows\Fonts\msyh.ttc`（脚本内已处理）
- 抽取工具 pdfplumber 0.11.7 / pypdf 6.14.2 / pymupdf；抽取命令见 `docs/AI_INDEX.md` §5

## 重要约定

- **Q1/Q2 数据一致**：`附件/result1.xlsx`、`result2.xlsx` 与 `model/*.md` 表、`src/*.py` 输出三者对齐（全量校验偏差仅 6 位小数舍入 ≤5e-7）。
- **碰撞判定**：SAT 天然覆盖"矩形完全包含"（缺陷1）；显式顶点包含作双保险；相邻板凳（编号差=1）经把手铰接，跳过不判碰撞；终止时刻用二分 + CCD 逼近。
- **码表注释风格**：代码是自包含数值脚本，含中文 docstring；图为 200 dpi。
