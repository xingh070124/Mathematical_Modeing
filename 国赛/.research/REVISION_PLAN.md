# 论文修订方案（回应三盲评审）

生成时间：2026-09-12 · 依据：`nature-reviewer` 三盲评审包（见 `.research/STATE.md` Session 26）
状态：**方案，尚未执行**。所有条目均给出位置、改法、是否需重算、风险与验收。

---

## 0 总览

| 批 | 内容 | 条目数 | 需重算 | 风险 |
|---|---|---|---|---|
| **A** | 必改（题面明文 + 三份共识） | 4 | 1 次（q4_produce） | 低 |
| **B** | 表述收紧（不改数字） | 4 | 无 | 低 |
| **C** | 需补算 | 7 | 4 次求解 + 2 处新诊断 | 中 |
| **D** | 需你决策（引入题面未给信息 / 重写论证） | 4 | 视选择 | 高 |

**关键事实**：A 批与 B 批**不改动任何已交付数值**。已逐项核实：
- 表 6 末行改在 $t_{\rm dry}$ 求值后，四位小数**四格全同**（0.1500/0.1413/0.1080/0.0526），仅底层未舍入值移动 $\sim3\times10^{-6}$。
- 「材料坐标」表补 21 列**不触及** `Sheet1`，故表 1–6 与四个结果文件的逐位一致不受影响。
- $t_{\rm dry}$ 位数统一**不改数值**，注册表已存 57.422337 / 51.085198。

---

## A 批 必改

### A1 补全 `result4.xlsx`「材料坐标」表至 21 列（三份共识）

- **位置** `src/q4_produce.py` 第 138–152 行
- **现状** 表头 21 列（0, 0.1, …, 2.0），实际只填 5 列（0/0.5/1.0/1.5/2.0），16 列 3065 行全空；末列表头写 `2.0` 而内容是 $r=R(t)$ 处的表面值
- **改法**
  1. 把传给 `sample_q4` 的 `r_out_cm` 由 5 个半径扩到 21 个（0, 0.1, …, 2.0）
  2. 21 列全部写出；表头改为 `0, 0.1, …, 1.9, 药材表面`
  3. 副题注一行：末列即初始位于 $2.0\,\mathrm{cm}$ 处的物质点，也就是当前药材表面
- **理由** 材料点在全部 21 个初始半径上都有定义（初始 $r_0$ 处物质点全程位于 $r_0R(t)/R_0$，$r_0=0.2\,\mathrm{cm}$ 时最小仅 $0.12\,\mathrm{cm}$），故这是一张**无空格**的完整表，与题面"0.1 cm 距离的完整结果"完全对应
- **需重算** 是（`python src/q4_produce.py`，约 1 分钟）
- **风险** 低。`Sheet1` 不动，故 `q4_reconcile.py` 前三关不受影响
- **顺带** `q4_reconcile.py` 增加第四关：逐列统计「材料坐标」表非空数，要求**每列 3065**
- **验收** 21 列非空计数全为 3065；末列表头为「药材表面」

### A2 表 6 末行改在 $t=t_{\rm dry}$ 求值（R3-M4 + R1-m7 共识）

- **位置** `src/q4_produce.py` 第 88 行 `row_dry = [t_dry/3600.0] + [s["Cg"][-1, j] for j in j5] + [s["Cs"][-1]]`
- **现状** `Cg[-1]` 是 60 s 网格**末点** 183900 s（51.0833 h）的值，而行标签写 $t_{\rm dry}=51.0852$ h。$t_{\rm dry}=183906.7128$ s **不在**该网格上
- **实测差** $C(0)$：183900 s 时 $0.15000345>0.15$，$t_{\rm dry}$ 时 $0.15000000$，差 $3.4\times10^{-6}$
- **改法** 末行改由 `res["sol"].sol(t_dry)` 取值并按 $\xi=r/R(t_{\rm dry})$ 插值；`registry_q4.csv` 中 `Q4_Ccenter`、`Q4_Csurf`、`Q4_Tsurf`、`Q4_Tmax` 的标注由"t_dry 时"改准确
- **需重算** 是（同 A1 一次运行）
- **风险** 低。**四位小数下四格全同**，表 6 印出来不变，改的是可追溯性
- **验收** 末行标签与取值时刻一致；`registry_q4.csv` 能查到该行的来源命令

### A3 $t_{\rm dry}$ 位数统一到四位小数（R3-M3 + R1-m7）

- **题面依据** `docs/A题.md` 第 20 行原文「所有结果保留四位小数（下同）」
- **现状四处不一致**（已逐一定位）

| 位置 | 现状 | 改为 |
|---|---|---|
| 摘要第 77 行 | $51.09\,\mathrm{h}$ | $51.0852\,\mathrm{h}$ |
| §5.4 框式第 1171 行 | $51.085\,\mathrm{h}$ | $51.0852\,\mathrm{h}$ |
| §5.3 框式第 914 行 | $57.42\,\mathrm{h}$ | $57.4223\,\mathrm{h}$ |
| 表 5 末行标签第 962 行 | $57.42$ | $57.4223$ |
| 表 9 基准行第 1089 行 | $57.4223$（**已合规**） | 不动 |
| 表 6 末行标签 | $51.0852$（**已合规**） | 不动 |

- **需重算** 否。注册表已存 `P01_tdry_h = 57.422337…`、`Q4_tdry_h = 51.085198…`
- **风险** 低。摘要改动会占约 2 个字符位，需重跑摘要单页门确认仍在第 1 页
- **待你选** 讨论性文字里的「约 57.4 h」「轴心 57.42 h」是否也改。**我的建议是不改**，并在 §5.1 加一句「头条答案按时效四位小数给出，讨论中的近似值以『约』标记」。若你要求全篇一律四位，我会一并改，但那会让「48.47 h」「55.24 h」这类也应跟着改，改动面变大且可读性下降

### A4 表 6 的 `\label{tab:q4}` 无 `\caption`（R3-m8）

- **位置** `paper/example.tex` 第 1219 行，`\label{tab:q4}` 位于 `\end{minipage}` 之后
- **现状** 在 `example.aux` 中解析为 `\caption@xref {tab:q4}{ on input line 1219}`，属 hyperref 诊断串；当前无人引用故 PDF 未受影响
- **改法** 删除该 `\label`（全篇 21 张表均为手工编号、无 `\caption`，保留它没有用途且是隐患）
- **需重算** 否
- **风险** 极低

---

## B 批 表述收紧（不改任何数字）

### B1 §5.4.1「唯一的收缩模式」（R1-M3，唯一被标阻塞项）

- **位置** 第 1135 行
- **现状**「这是在不引入额外自由参数的前提下唯一的收缩模式：任何非均匀模式都需要假定一个空间分布，而题面并未给出这种数据」
- **问题** 单参数非均匀族同样零自由参数（R1 给的构造：$R(r,t)/R_0=1-(1-R(t)/R_0)g(C(r,t))$，$g$ 任意）。「唯一」在数学上不成立，且被用来支撑 51.0852 h 这个具体数字
- **改为**

  > 题面只给出表面半径序列，内部位移场在给定数据下不可辨识。本文取**均匀（仿射）收缩**作为基准模式：它不需要假定任何题面未给的空间分布，且是唯一能直接由 $R(t)$ 确定物质坐标的模式。非均匀模式需引入形状函数，其影响由本章末的收缩模式对照界定（$24.3\sim129.8\,\mathrm{h}$），因此 \S5.4.3 的 $t_{\rm dry}$ 应理解为**均匀收缩假设下**的值。

- **需重算** 否
- **风险** 低。**这是消除唯一阻塞项的最低成本做法**
- **待你定** 是否进一步做模式包络（见 D1）。若做包络，此处还要把「基准模式」改为「区间内的一个点」

### B2 摘要「独立有限体积格式」→ 与 §6.4.1 自述对齐（R2-M3/M4）

- **位置** 第 77 行
- **现状**「独立有限体积格式与主模型温度一致到 $2\times10^{-5}\,\mathrm{K}$，积分到终止事件的外推极限相差 $0.0016\,\mathrm{h}$」
- **问题** §6.4.1 已自述该格式**共享物质坐标与干物质守恒假设**，故只验证离散与实现，不验证坐标变换本身。摘要的表述强度高于正文自述
- **改为**「另一套独立**实现**（单元中心有限体积，与本文共享坐标假设）在温度上一致到 $2\times10^{-5}\,\mathrm{K}$，独立积分到终止事件后的外推极限相差 $0.0016\,\mathrm{h}$」
- **需重算** 否
- **风险** 低。摘要变长约 12 字，需确认仍在第 1 页

### B3 图 3(b) 题注补基准（R2-m7 的存活部分）

- **位置** 第 627 行题注「守恒形式伪源项占主项 $18.45\%$」
- **问题** 该百分比依赖参考温度零点（$T_{\rm ref}$ 取 0 / 273.15 / 301.15 K 时守恒形式伪源项与主项之比依次为 10.81 / 95.52 / 485.97）。题注未写基准
- **改为**「守恒形式伪源项占主项 $18.45\%$（基准 $T_{\rm ref}$ 取初温 $28\,^\circ\mathrm{C}$；该比值随零点改变，故正文不以它为判据，见 \S5.2.2）」
- **需重算** 否
- **说明** R2-m3 关于「论文一边否定 1.71% 一边又用它」的断言**经核不成立**：字面 `1.71` 全文仅 1 处（第 534 行），恰在**否定**它的那句里；另一处命中是表 8 里的 `1.7183`。故 B3 只处理题注缺基准

### B4 Jacobian FD 核验的措辞统一（R2-m5）

- **位置** 第 646 行（$2.25\times10^{-7}$）、第 867 行（$2.24\times10^{-7}$）、附录 E.3
- **问题** 措辞像在报告 Jacobian 精度，实际该量级等于中心差分自身的截断误差，故只能排除 $O(1)$ 错误
- **改为**统一为「该核验只确认 Jacobian 无 $O(1)$ 错误，其分辨率受中心差分截断误差（$\sim3\times10^{-7}$）限制；偏差随步长按 $h^2$ 下降（相邻比值 4.001/3.989/4.014）证明这一点」
- **需重算** 否（步长序列已有）

---

## C 批 需补算

| # | 内容 | 来源 | 位置 | 需做什么 | 成本 |
|---|---|---|---|---|---|
| C1 | 问题一、二**忽略收缩**的量化 | R1-M1 | 假设 7 后 或 §5.1.5 | 取等效常半径 $\sqrt{R_0R(1800)}=1.935459\,\mathrm{cm}$ 重算问题一，报告表 1、表 2 各 35 位的最大偏差与体积平均含水率下降量的变化 | 1 次求解 |
| C2 | 体积平均量的**归一化口径** | R1-M1 | §4.1 报「下降 10.1%」处 | 补一句：该值在**单位初始截面积** $\pi R_0^2$ 上归一化；换算到单位当前截面积即 $10.1158\%\times0.877032=8.8719\%$，差 1.2439 个百分点 | 无（已算） |
| C3 | $L_v$ 对问题三、四 $t_{\rm dry}$ 的影响 | R3-m6 | 表 13 | 问题四已在注册表（$L_v=0$ 时差 $-32.7\,\mathrm{s}$，即 $-0.0091\,\mathrm{h}$）；**问题三需补一次求解** | 1 次求解 |
| C4 | 界面扩散系数**算术 vs 调和平均** | R1-m1 | §5.1.4 步骤 4 | 同网格同步长重算问题一，报告表 1、表 2 变化量 | 1 次求解 |
| C5 | 非保守形式的**离散能量收支残差** | R1-M4 | §5.2.6 | 写一个诊断：净边界热流对时间积分 vs 蓄热变化，报相对残差 | 写脚本 |
| C6 | §5.4.2 事件**唯一性论证**重写 | R2-M5 | 第 1147 行附近 | 删掉以严格单调为前提的 $\varepsilon$ 论证，改为：① 实测 $\max_iC_i(t)$ 全程单调不增（逐 BDF 步差分，最大正增量 0）；② 说明 $C(0)-C(0.1\,\mathrm{cm})$ 早期恰为 0 时事件检测依赖稠密输出上的变号，并给出该情形下的定位区间 | 写脚本 |
| C7 | $\rho_s$ 变化 2.12 倍写入正文 | R1-m4 | §5.2.2 | 数据已在注册表（$275.04\to581.91\,\mathrm{kg/m^3}$）；把 $1.93\times10^{-16}$ 改述为「给定 $\rho_s$ 下的代数残差」并写出该变化幅度 | 无（已算） |
| C8 | 末期「与环境完全平衡」收紧 | R1-m5 | 第 977 行 | 改为「与环境之差为毫开尔文量级（$T_\infty-T_R\approx L_vh_m(C(R)-C_\infty)/h$，$t_{\rm dry}$ 时约 $2\times10^{-4}\,\mathrm{K}$）」 | 无（已算） |
| C9 | 表 20 补登第二张工作表 + 精度表述 | R2-m6/R3-m3 | 附录 G.1 | 补「材料坐标」表一行；把「所有数值保留四位小数」改为「论文表 1–6 保留四位小数，交付文件按模板写入」 | 无 |
| C10 | 符号表补 5 个符号 | R3-m4 | §3 | 补 $\xi$、$\beta$、$C^{*}$、$t_{\rm dry}$、$R(t)$ | 无 |
| C11 | $1.198$ 与 $1.200\,\mathrm{cm}$ 各加时刻限定 | R3-m5 | 摘要、§4.4、§5.1、表 6 表注 | 前者标「附件 2 末点（$72\,\mathrm{h}$）」，后者标「$t_{\rm dry}$ 时」 | 无 |

---

## D 批 需你决策

### D1 问题四收缩模式：处置到哪一层

| 选项 | 做法 | 成本 | 效果 |
|---|---|---|---|
| **(a) 只改表述** | 即 B1，并把摘要与结论的 51.0852 h 写成「均匀收缩假设下」 | 极低 | 消除 R1-M3 的阻塞名义，但区间仍在，读者自判 |
| **(b) 加模式包络** | 构造 2–3 个单参数非均匀模式（按局部含水率、按初始半径两极端），报 $t_{\rm dry}$ 完整区间，把 51.09 h 改述为区间内一点 | 中（每模式 1 次求解） | 把「不可辨识」升格为「有界区间」，是最实质的补强 |
| **(c) 论证最大值位置无关** | 论证「全场最大值恒在轴心」与收缩模式无关，则模式只经 $D$ 影响 $t_{\rm dry}$ | 中 | 若成立，可用最少的假设消掉整条关切 |
| **(d) 不动** | 维持现状 | 零 | 保留唯一阻塞项 |

**我的建议**：(a) 无条件做；(b) 或 (c) 至少选一个。(c) 更漂亮但有失败风险（非均匀收缩会改变干物质的空间分布，轴心未必仍是最大值所在）。**(b) 稳妥**，且与论文已有的 $\eta$ 族同源，数据现成。

### D2 恒温阶段烘房湿度 $C_\infty$ 是否闭合

- **选项** (a) 明确列为显式假设并给出维持 $0.0500$ 所需的最小排湿速率；(b) 引入烘房有效容积/换气率，把 $C_\infty(t)$ 解成自洽的
- **提示** (b) 需要**题面未给**的两个参数（烘房体积、换气率），与「不使用题面未给的参数」的要求冲突，故我倾向 **(a)**
- **(a) 的具体内容**：由累计失水量与目标 $C_\infty$ 反推所需换气稀释速率，写成一句「若烘房有效容积为 $V$、排湿使 $C_\infty$ 稳定在 $0.0500$，则所需最小排湿速率为 $X$」，并注明 $X$ 依赖 $V$、题面未给 $V$

### D3 `result2.xlsx` 的时间上界

- **现状** 3 h（10800 s，10800 行）
- **题面** 问题二只在表 3、表 4 处写「3 h 内每隔 0.5 h」，对文件只说「每隔 1 s…的完整结果」，**未给上界**
- **我的判断** 3 h 有据：问题一的平行句式是「并将 **1800 s 内**每隔 1 s…的完整结果保存到 result1.xlsx」，即**文件窗口 = 表格窗口**；按同一句式，问题二的文件窗口应等于表 3/表 4 的 3 h
- **选项** (a) 在 §4.2 或附录 G.1 加一句依据（1 行，推荐）；(b) 把文件扩到整个烘干过程（$259200\,\mathrm{s}$ 需 259200 行 × 21 列，文件约 40 MB）并说明
- **建议** (a)

### D4 $t_{\rm dry}$ 位数是否全篇一律四位

- **选项** (a) 仅头条答案四位 + §5.1 一句口径说明（推荐）；(b) 全篇凡报结果处一律四位
- **影响** (b) 会波及「48.47 h」「55.24 h」「$2.4$ 天」「$51.1\,\mathrm{h}$」等数十处，且部分处本就写作近似值

---

## 执行顺序与验收

1. **A1 + A2** 一起做（同一次 `q4_produce.py` 运行），随后 `python src/q4_reconcile.py` 四关全过
2. **A3 + A4 + B1–B4** 一次定点写回（用 `src/_abs_apply.py` 那种「唯一性断言 + 备份 + 回读校验」的脚本，避开并发写入）
3. **C1–C11** 分批，每批后重跑数字对账门
4. **D1–D4** 按你的选择再做

**每批后必跑**：`q2_reconcile.py`（未对上 0）、`q4_reconcile.py`（四关）、
`q2_verify_delivery.py`（编译干净 / 摘要单页 / 四问篇幅 / 图件门）。
**注意**：本工作区仍有并发写入，改 `paper/example.tex` 前后都要看 mtime。

---

## 建议不改的项（附理由）

| 项 | 谁提的 | 不改的理由 |
|---|---|---|
| 表 6 的 1.5 cm 列记「—」 | R3-M1 视作缺口 | **R1 判为正确**：「以'—'表示该位置已在药材之外，这一处理有表注说明且正确」。题面「到药材中心距离」的字面读法是物理距离，该位置确实不存在。A1 已保证材料点口径的数据在文件里可得，无需改动表 6 |
| 把 $t_{\rm dry}$ 改为 M=3200 或外推值 | R2-M2 的两个选项之一 | 现口径（M=200 的生产解 + 明确给出 ±0.011 h）已经声明；改为外推值需重做表 5、表 6、result3/4 与全部对账，成本远超收益。**建议改为在表 13 把「空间离散 ±0.011 h」的措辞由"不确定度"改为"生产网格相对外推极限的系统偏差"**（1 行改动，采纳 R2-M2 的第二个选项），我把它并入 C 批执行 |
| 表 17 的步数比 | R2-m1 | **经核为误报**：表写 $2.78\times10^3$／$2.34\times10^3$，注册表 `EXP_q3_Nratio=2778.9`／`EXP_q4_Nratio=2334.9`，一致 |
| 1.71% 的前后不一致 | R2-m3 | **经核为误报**：全文仅 1 处，恰在否定它的那句里。仅按 B3 补图注基准 |
| 「材料坐标」表 5 列与 Sheet1 重复 | R2-M1 的证据 | **经核为误报**：0.5 cm 列 3065 行中 3000 行不同 |
| Sheet1 的 36% 空格另有空域 | R2-M1 的证据 | **经核为误报**：23159 个空格全部由 $r>R(t)$ 解释，未解释 0 |
